apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "rh-sso.fullname" . }}-test-connection"
  labels:
    {{- include "rh-sso.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "rh-sso.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
